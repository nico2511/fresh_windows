# Legacy STT (quarantine)

Scripts WhisperLoop / MCI 4s — **ne plus utiliser**.

Remplace par `voice_worker.exe` (asset FreshWindows) + `FreshAgent-VoiceWorker.ps1`.

Conserves pour reference historique uniquement ; non charges au boot.
